[workspace]
resolver = "2"

members = [
    "cyberkernel",
]

# Workspace-wide profile settings
[profile.release]
# Optimize fully for bare-metal / embedded targets
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
